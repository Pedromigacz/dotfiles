# any file named auth.json, at any depth
auth.json

# any directory named sessions
sessions/
agent/settings.json

# any .lock file
*.lock

# any directory named npm
npm/

# installed type-check deps for extensions (pi provides these at runtime)
node_modules/
