# any file named auth.json, at any depth
auth.json

# any directory named sessions
sessions/
agent/settings.json

# any .lock file
*.lock

# any directory named npm
npm/

# editor-only type deps for extensions (re-create with: bun install)
node_modules/
